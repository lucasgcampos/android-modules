<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lucascampos.modules.pullrequest">

    <application>

        <activity android:name=".feature.PullRequestActivity">
            <intent-filter>
                <action android:name="lucascampos.modules.PullRequestActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>

</manifest>
